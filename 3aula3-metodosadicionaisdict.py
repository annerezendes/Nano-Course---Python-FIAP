import os
os.system("cls")

"""
Métodos Dicionários:

.items() = retorna chave e valor juntos (todos)

.values() = retorna todos os valores

.keys() = retorna todas as chaves

.clear() = zera o dicionário

.pop() = tira o item do dicionário e atribui a alguma variável para trabalhar o item, e depois pode ser descartada


"""